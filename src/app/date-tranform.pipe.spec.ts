import { DateTransformPipe } from './date-tranform.pipe';

describe('DateTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransformPipe();
    expect(pipe).toBeTruthy();
  });
});